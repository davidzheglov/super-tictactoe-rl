# Ubuntu remote GPU training dependencies.
# Install inside a virtual environment on the server.
#
# PyTorch itself is installed by setup_remote_gpu.sh from the official CUDA
# wheel index so it gets a GPU-enabled build instead of a CPU-only wheel.
numpy==1.26.4
pandas==2.2.2
matplotlib==3.8.4
gymnasium==1.1.1
tqdm>=4.66
torchrl>=0.6
tensordict>=0.6
urllib3<2
